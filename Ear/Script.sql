--<ScriptOptions statementTerminator=";"/>

CREATE TABLE user (
	iduser INT NOT NULL,
	name VARCHAR(45)
);

CREATE UNIQUE INDEX PRIMARY ON user (iduser ASC);

CREATE UNIQUE INDEX PRIMARY ON user (null);

CREATE UNIQUE INDEX PRIMARY ON user (null);

